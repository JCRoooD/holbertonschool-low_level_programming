#include <stdio.h>
/**
 * main - Entry point
 *
 * Result: Always 0
 */
int main(void)
{
	char AB;
	for (AB = 'a' ; AB <= 'z' ; AB++)
		putchar (AB);
	for (AB = 'A' ; AB <= 'Z' ; AB++)
		putchar (AB);

	putchar ('\n')
		return (0);
}
