#include <stdio.h>
/**
 * main - entry point
 * Return: Always 0
 */
int main(void)
{

	char x;

	for (AB = '0' ; AB <= '9' ; AB++)
		putchar (x);
	for (AB = 'a' ; AB <= 'f' ; AB++)
		putchar (x);

	putchar('\n')
		return (0);
}
