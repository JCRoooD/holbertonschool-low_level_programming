#include "main.h"
#include <stdio.h>
/**
 * mul - multiplication of two integers
 * Return: a multiplied to b
 * @a: first integer
 * @b: second integer
 */
int mul(int a, int b)
{
	int i;

	i = (a * b);
}
