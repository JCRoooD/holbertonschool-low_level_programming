#include "main.h"
#include <stdio.h>
/**
 * add -  print result of the sum
 * Return: sum of the two integers
 * @q: first integer
 * @p: second integer
 */

int add(int q, int p)
{
	int x;

	x = q + p;

	return (x);
}
