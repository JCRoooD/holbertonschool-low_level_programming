#include "main.h"
#include <stdio.h>
/**
 * add - print the sum of the integers
 * Return: sum of the two integers
 * @q: first integer
 * @p: second integer
 */

int add(int q, int p)
{
	int x;

	x = q + p;

	return (x);
}
